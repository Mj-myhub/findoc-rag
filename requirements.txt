# ============================================================
# FinDocRAG dependencies
#
# Phase 1 (now):  pip install -r requirements.txt
# Later phases:   uncomment the relevant block when you reach that week.
#
# Splitting installs by phase keeps Phase 1 fast and avoids
# dependency conflicts you do not need to deal with yet.
# ============================================================

# --- Phase 1: Foundation (needed now) ---
datasets>=2.19
pandas>=2.0
pyarrow>=15.0
pyyaml>=6.0
python-dotenv>=1.0

# --- Dev tooling (needed now) ---
pytest>=8.0
ruff>=0.5

# --- Phase 2: RAG core (uncomment in Weeks 3-6) ---
# sentence-transformers>=3.0
# rank-bm25>=0.2.2
# chromadb>=0.5
# groq>=0.9

# --- Phase 3: Evaluation + observability (uncomment in Weeks 7-8) ---
# ragas>=0.1.7
# deepeval>=0.21
# langfuse>=2.0

# --- Phase 4: Serving + demo (uncomment in Weeks 9-10) ---
# fastapi>=0.110
# uvicorn>=0.29
# streamlit>=1.35
